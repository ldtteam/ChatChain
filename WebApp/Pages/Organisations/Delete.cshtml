@page "/Organisations/{organisation}/Delete"
@model WebApp.Pages.Organisations.Delete
@{
    ViewData["Title"] = "Delete";
}
<div class="container">
    <h3>Are you sure you want to delete this Organisation?</h3>
    <div>
        <hr/>
        <dl class="dl-horizontal">
            <dt>
                <p>Name:</p>
            </dt>
            <dd>
                @Html.DisplayFor(m => m.Organisation.Name)
            </dd>
        </dl>
        <form method="post">
            <input type="hidden" asp-for="Organisation.Id"/>
            <input type="submit" value="Delete" class="btn btn-danger"/>
            <a asp-page="./Index" class="btn btn-success">Back to List</a>
        </form>
    </div>
</div>