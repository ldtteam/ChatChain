@page "/{organisation}/Clients/{client}/Delete/"
@model DeleteModel
@{
    ViewData["Title"] = "Delete";
    Layout = "Shared/_ClientGroupsLayout";
}
<div class="container">
    <h2>Delete</h2>

    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>Client</h4>
        <hr/>
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(m => m.Client.Id)
            </dt>
            <dd>
                @Html.DisplayFor(m => m.Client.Id)
            </dd>
            <dt>
                @Html.DisplayNameFor(m => m.Client.Name)
            </dt>
            <dd>
                @Html.DisplayFor(m => m.Client.Name)
            </dd>
            <dt>
                @Html.DisplayNameFor(m => m.Client.Description)
            </dt>
            <dd>
                @Html.DisplayFor(m => m.Client.Description)
            </dd>
        </dl>

        <form method="post">
            <input type="hidden" asp-for="Client.Id"/>
            <input type="submit" value="Delete" class="btn btn-danger"/>
            <a asp-page="./Index" asp-route-organisation="@Model.Organisation.Id" class="btn btn-success">Back to List</a>
        </form>
    </div>
</div>