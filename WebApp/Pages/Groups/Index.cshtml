@page
@model GroupsModel

@{
    ViewData["Title"] = "Groups";
}

<div class="container">
    <h2>@ViewData["Title"]</h2>

    <p>
        <a asp-page="./Create">Create New</a>
    </p>

    <div class="row">
        <table class="table" style="color: white">
            <thead>
            <tr>
                <th>Database Id</th>
                <th>Group Name</th>
                <th>Group Id</th>
                <th>Group Description</th>
                <th>Clients</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            @foreach (var group in Model.Groups)
            {
                <tr>
                    <td>
                        @Html.DisplayTextFor(m => group.Id)
                    </td>
                    <td>
                        @Html.DisplayFor(m => group.GroupName)
                    </td>
                    <td>
                        @Html.DisplayFor(m => group.GroupId)
                    </td>
                    <td>
                        @Html.DisplayFor(m => group.GroupDescription)
                    </td>
                    <td>
                        <a asp-page="./Clients" asp-route-id="@group.Id.ToString()">View Clients</a>
                    </td>
                    <td>
                        <a asp-page="./Edit" asp-route-id="@group.Id">Edit</a>
                        <a asp-page="./Delete" asp-route-id="@group.Id">Delete</a>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>